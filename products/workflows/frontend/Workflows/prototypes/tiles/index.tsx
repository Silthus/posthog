// PROTOTYPE (throwaway): stub for the tiles variant. The tiles variant worker owns this directory.
import { PrototypeComingSoon } from '../shared/PrototypeComingSoon'

export function TilesVariant(): JSX.Element {
    return <PrototypeComingSoon name="tiles" />
}
