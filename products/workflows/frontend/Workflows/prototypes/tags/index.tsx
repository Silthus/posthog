// PROTOTYPE (throwaway): stub for the tags variant. The tags variant worker owns this directory.
import { PrototypeComingSoon } from '../shared/PrototypeComingSoon'

export function TagsVariant(): JSX.Element {
    return <PrototypeComingSoon name="tags" />
}
