// PROTOTYPE (throwaway): stub for the folders variant. The folders variant worker owns this directory.
import { PrototypeComingSoon } from '../shared/PrototypeComingSoon'

export function FoldersVariant(): JSX.Element {
    return <PrototypeComingSoon name="folders" />
}
