{
    "name": "workflows-as-code-prototype",
    "private": true,
    "type": "module",
    "description": "Throwaway prototype. Not part of the pnpm workspace and not shipped.",
    "scripts": {
        "emit": "bun run example/onboarding.workflow.ts",
        "push": "bun run example/onboarding.workflow.ts --push",
        "evidence": "bun run dev/record-transcript.ts",
        "typecheck": "bunx tsc --noEmit",
        "identity": "bun run dev/identity-transcript.ts"
    },
    "devDependencies": {
        "@types/bun": "^1.4.2"
    }
}
